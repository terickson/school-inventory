We need to create an appplication that allows teachers to inventory their supplies.  The teachers have storage closets (locators) and sometimes shelves (sublocators) where they put specific items of quanity (no tracked asset tags) they store in those locations/sublocations.  We will need an API with a database (use sqllite).  It will need to have its own user/security system...we can prepoulate an admin with a config that has the admin password in it.   We will need UI that is for admins to manage the setup of users and then we will need users to setup storage locators and sub locators and a item catelog.  We also want a checkout system so that users can checkout inventory for a specific length of time.  This should all be ractive so it can be used on a laptop or phone.
I would like to use agent teams for this.  I need 1 expert who understands software technologies avaiable for teacher inventory systems to compare our system against and make sure our features make since.  I need a python api expert who understands how to setup a good restful api in python with strong swaggar documentation.  I need a database expert who knows good sqlite pratices.  I also need a python best practices agent that will review the api plan to make sure it follows best practices.  I need Vue3 UI expert.  I need a UI Designer who is knowledgeable in CSS, HTML, Vuetify and material design.  I need a usabiilty expert to make sure the UI is being built with usability in mind.  I will also need a Implementer to create the application.
I would fist like the agents to get together and come up with a plan for creating this application.  I would like a seperate API from UI. I would like tests and documentation to be considered in the plan.  Also I would like user profiles and user stories that the plan is addressing.  After the plan is put together I would like to look over the plan and once I approve of the plan we should move on to implementation.
Implementation phase should be to create the API and database first run the tests to make sure the api is in a good state and then create the UI and run tests for it and fix any issue.  After we have run the tests. Create puppeteer tests for the user stories and run both the api and the UI and run through the puppteer tests and fix any issues you find.  We should have a script to reset the database if we need to so we can re-run the puppeteer tests until they completely pass.  Also we should try to fix and console warning or errors found during the tests.  At the end we should also create a docker compose file that we can use to run this in prod and create a README.md with complete documentation. 